# 基于多模态记忆的自治叙事引擎

**本地原创小说推演器架构方案**

---

### 1. 系统核心架构图 (The Architecture)

这个系统由 **Go (中枢调度)**、**C++/llama.cpp (推理算力)** 和 **多维数据库 (记忆存储)** 三部分组成。

- **前端 (Vue.js)**：作为“驾驶舱”，展示大纲流、推演盘、角色状态卡片。
- **后端 (Go)**：**逻辑大脑**。负责从各个库里“抓药”，组装 Prompt，管理生命周期。
- **推理层 (C++)**：**算力心脏**。提供 LLM 推理接口 (llama-server) 和 Embeddings 向量化接口。

---

### 2. 六大核心库的职能与存储方案

为了让 Go 能高效读写，我们按数据特性把库分为 **关系型 (SQLite)** 和 **向量型 (ChromaDB/Faiss)**。

| **库名称** | **存储技术** | **核心职能** | **更新频率** |
| --- | --- | --- | --- |
| **1. Flow 节点库** | SQLite | 存储大纲树、章节目标、字数预算 | 低 (用户手动改) |
| **2. 事件/事实库** | SQLite | 记录已发生的“既定事实”、虚拟时间线 | 高 (每章自动增) |
| **3. 伏笔/线索库** | SQLite | 状态机管理 (埋坑/填坑/废弃) | 中 (随剧情变动) |
| **4. 实体百科库** | Vector DB | 存储世界观名词 (物品/地点) + 向量索引 | 低 (初始化+偶发) |
| **5. 规则法则库** | Vector DB | 存储物理/魔法铁律 (用于纠错) | 极低 (基本静态) |
| **6. 角色矩阵库** | SQLite + Graph | 存储好感度、当前状态 (HP/MP)、位置 | 高 (每章变动) |
| **7. 生成式大纲库** | SQLite | 承上启下，将宏观的一句话目标拆解为具体的“动作指令”，它是 RAG 检索的**真正输入源**。 | 中（每章生成前由 Planner AI 规划，用户可微调） |

---

### 3. 核心推理全流程 (The Inference Pipeline)

当你在前端点击 **“生成本章”** 时，Go 后端会启动一个 **“四阶段流水线”**。

### 阶段一：上下文组装 (Context Assembly) —— 核心是“按需加载”

Go 后端需要像拼乐高一样，从六大库里提取最相关的信息。

1. **大纲定位**：从 **Flow 库** 读取当前章节目标（如：“墨离在禁地遭遇守护兽”）。
2. **关键词预测 (Planner)**：
    - 调用小模型快速分析大纲，提取关键词：`[禁地, 守护兽, 墨离, 战斗]`。
3. **向量检索 (RAG)**：
    - 用关键词去 **实体百科库** 搜：检索“禁地环境”、“守护兽弱点”。
    - 用关键词去 **规则法则库** 搜：检索“战斗规则”、“禁地限制”。
4. **状态快照**：
    - 从 **角色矩阵库** 获取墨离当前状态（重伤？满血？）。
    - 从 **伏笔库** 检索所有状态为 `Active` 且关联当前场景的线索（如：手里捏着还没用的必杀技）。
5. **事实回溯**：
    - 从 **事实库** 拉取最近 3 章的摘要（短期记忆）和前文的关键转折（长期记忆）。

**最终产物**：一个包含世界观、规则、当前状态、任务目标的 **Mega-Prompt**。

### 阶段二：正文推演 (Execution)

1. **流式生成**：Go 将 Mega-Prompt 发送给 `llama-server`。
2. **实时展示**：前端通过 WebSocket 逐字显示生成的正文。
3. **动态干预 (可选)**：如果生成过程中出现了特定关键词（如“使用了禁术”），Go 可以实时触发规则检查，如果违反 **规则库**，立即中断并让模型重写。

### 阶段三：观察与提取 (The Observer) —— 库的自动化更新机制

这是系统“活”起来的关键。正文生成完毕后，Go 会启动 **“记录员 Agent”**（可以是一个专门的 Prompt），负责分析刚刚生成的文本。

**Extraction Prompt 示例：**

> “阅读上文，以 JSON 格式输出：
> 
> 1. 新发生的关键事件（事实库）；
> 2. 是否有伏笔被回收或新埋下（线索库）；
> 3. 角色的位置、好感度、属性变化（角色库）；
> 4. 是否创造了新的专有名词（百科库候选）？”

### 阶段四：数据回写与冲突检测 (Commit & Check)

Go 接收到 JSON 后，执行以下逻辑：

1. **更新事实库**：写入新事件，推进虚拟时间线。
2. **更新角色库**：修改墨离状态（如：HP -30，位置->山洞）。
3. **更新线索库**：将“必杀技”线索标记为 `Resolved`；新增“受伤的伏笔”为 `Active`。
4. **百科库扩容 (需审核)**：如果 AI 编造了新怪物，弹窗询问用户：“检测到新生物‘冰原狼’，是否将其存入百科库？”用户确认后，向量化入库。

---

### 4. 关键技术难点与解决方案

### A. 怎么解决“模型不知道该用哪个百科”？

**方案：双层 RAG。**

- 第一层：根据 **Flow 大纲** 检索。大纲里有“草药课”，就去拉草药相关百科。
- 第二层：根据 **正文生成流** 检索。如果写到一半模型突然写了“他拿出了那本古书”，Go 后端立即在后台检索“古书”的设定，并动态插入到后续的 Context 中（这叫 Dynamic Context Injection）。

### B. 怎么保证“逻辑不崩坏”？

**方案：Critique 拦截器。**

在写入数据库之前，引入 **Critic Agent**。

它拿着 **规则库** 对比 **新生成的事件**。

- *检查*：新事件“墨离飞起来了” vs 规则“禁空领域”。
- *结果*：如果冲突，自动回滚，并在 Prompt 中加入错误提示让 Writer 重写。

### C. 修改大纲后，后面的库怎么办？

**方案：级联失效 (Cascading Invalidation)。**

- 当你在 **Flow 库** 修改了第 5 章。
- Go 自动查找 **事实库** 中所有 `Chapter >= 5` 的记录，标记为“失效”。
- **角色库** 回滚到第 5 章开始时的快照。
- **线索库** 中在第 5 章之后埋下的伏笔全部删除。

---

### 5. 你的开发路线图 (Roadmap)

建议分三步走：

**第一阶段：骨架 (Skeleton)**

- 实现 Go + llama.cpp 的流式对话。
- 实现 **Flow 库** (大纲) 和 **事实库** (正文存储)。
- 能跑通：输入大纲 -> 生成正文 -> 存入数据库。

**第二阶段：记忆 (Memory)**

- 引入 **实体百科库** (ChromaDB)。
- 实现 Go 的关键词提取 + 向量检索逻辑。
- 让模型能写出符合设定的东西。

**第三阶段：灵魂 (Soul)**

- 引入 **角色矩阵** 和 **伏笔库**。
- 实现“观察者 Agent”的自动提取 JSON 逻辑。
- 实现状态自动更新，让小说真正“活”起来。

这个方案利用了你 Go 后端的高并发能力来处理繁杂的数据调度，让 llama.cpp 专注于它最擅长的事情——基于给定的完美上下文写出漂亮的文字。